{
  "name": "tab-yeet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Firefox extension: copy tab URLs with transforms and formatting",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "vitest": "^3.0.0"
  }
}
