# Active Context

## Current Task

Drag-to-reorder for regex replacement rules on the options page (replacing Up/Down buttons).

## Phase

REFLECT — complete for task `drag-reorder-rules`.

## What Was Done

- **Complexity:** Level 2 — simple enhancement, self-contained to `options/` + `test/options/`.
- **Implementation:** `reorderRules` in `options/options.js`; drag handle + list-level DnD; styles in `options/options.css`; tests in `test/options/options.test.js`.
- **QA:** PASS — `memory-bank/active/.qa-validation-status`.
- **Reflection:** `memory-bank/active/reflection/reflection-drag-reorder-rules.md`.

## Next Step

Operator may run `/niko-archive` to archive and remove `memory-bank/active/` when satisfied.
